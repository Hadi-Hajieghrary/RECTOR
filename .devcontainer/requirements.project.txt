# Project-specific Python packages for RECTOR Closed-Loop Experiments
# ===================================================================
# Add new Python packages here - this file is loaded AFTER the base requirements,
# so changes here won't invalidate the cached layers for PyTorch, TensorFlow, etc.
#
# NOTE: JAX[cuda12] and Waymax are installed in dedicated Dockerfile layers
# (LAYER 6.5 and 6.6) for better caching. Only add NEW experiment-specific
# packages below.

# ============================================================
# Statistical Analysis & Experiment Reporting
# ============================================================
pandas>=1.5.0,<3.0
statsmodels>=0.14.0,<1.0
# Bootstrap confidence intervals (paired tests, Holm-Bonferroni)
bootstrapped>=0.0.2

# ============================================================
# Experiment Configuration & Reproducibility
# ============================================================
hydra-core>=1.3.0,<2.0
omegaconf>=2.3.0,<3.0

# ============================================================
# Data Processing / Serialization
# ============================================================
pyarrow>=14.0.0,<18.0

# ============================================================
# Additional Visualization (interactive + publication-quality)
# ============================================================
plotly>=5.18.0,<6.0
# CairoSVG for exporting vector figures from matplotlib
cairosvg>=2.7.0,<3.0

# ============================================================
# Video Production (annotation overlays on BEV movies)
# ============================================================
imageio[ffmpeg]>=2.33.0,<3.0
moviepy>=1.0.3,<2.0
